#!/bin/bash -ex

brew_home=$(brew --prefix)
dir1="$brew_home/var/postgres94/"
dir2="$brew_home/var/postgres/"

if [ -d "$dir1" ]; then
  postgres -D "$dir1"
  exit
fi

postgres -D "$dir2"

# vim: ft=sh
